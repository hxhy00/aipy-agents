/server/
/node_modules/
/package.json
/package-lock.json
bun.lock
.gitignore
.DS_Store
*.dxt
*.mcpb
README.md
/smoke-test.mjs
/protocol-check.mjs
/sample-*.md
/doc-video-output/
/industry-research-output/
# 开发工具脚本，不随包分发
/scripts/
# 旧图标：manifest 引用的是 icon.png（打包器强制要求 PNG 格式）
icon.svg
# ffmpeg 二进制不随发布包分发（三平台全量约 126MB，打包后超过 GitHub Release 100MB 硬限制）。
# 改由用户自行安装：macOS `brew install ffmpeg`，Windows `winget install Gyan.FFmpeg`。
# 插件侧 server/lib/binaries.js 会自动在 PATH 与常见安装路径中查找，无需改配置。
# 说明见 vendor/README.md。
/vendor/
