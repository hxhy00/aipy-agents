interface:
  display_name: "公众号文章排版 SOP"
  short_description: "终稿（Word/Markdown）→ 选模板 → 排版生成 HTML → 预检 → 发布到公众号草稿箱"
  default_prompt: "Use $wechat-article-sop-layout to take my Word/Markdown draft all the way to the WeChat Official Account draft box: parse the manuscript, pick a style, generate the WeChat HTML, pre-check it, then publish. 按全流程 SOP 执行，确定性步骤用 scripts 下的脚本，排版由你完成。"
policy:
  allow_implicit_invocation: true
