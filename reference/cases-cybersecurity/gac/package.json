{
  "name": "@aipy-pro/gac",
  "private": true,
  "dependencies": {
    "@modelcontextprotocol/sdk": "*",
    "common": "file:../common",
    "p-limit": "^7.2.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "prepack": "bun build --outfile=server.js --production --target=node ./server/index.js",
    "pack": "dxt pack",
    "postpack": "bash ../../postpack.sh",
    "postversion": "jq \".version = $(jq .version package.json)\" manifest.json > manifest.json.tmp && mv manifest.json.tmp manifest.json && npm run pack"
  }
}
